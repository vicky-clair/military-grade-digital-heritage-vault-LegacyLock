# 所有者使用手册 · LVCF 3

## 创建和解锁

1. 首次启动，设置至少 12 位密码，重复输入确认。
2. 点击生成安全密钥，将完整 `LL3-…` 密钥保存在独立安全位置，确认已备份。重新生成会取消备份确认。
3. 创建本机密库。密码、密钥不保存在 localStorage、IndexedDB 或明文设置中。
4. 每次重新启动、锁屏或休眠后，均须同时输入密码和密钥。旧版“仅 PIN”或“仅密钥”解锁已停用。

两项凭据都丢失时，已配置的两盘仍可恢复只读数据；两盘不能恢复所有者签名权限。未配置两盘且凭据丢失，没有后台重置入口。

## 资产与应用设置

“资产”页可新建、编辑、删除、搜索和按分类筛选。保留原有 24 类资产、自定义字段和附件。单附件最多 2 MB，资产数据总量最多 20 MB，密库文件最多 32 MB。保存失败会显示错误并保留编辑窗口，不能把关闭窗口当作成功保存。

所有者的“设置与备份”页可设置 1/5/15/30/60 分钟自动锁定，以及继承人姓名和说明。系统休眠、系统锁屏、窗口关闭会清除当前授权；继承会话拔盘也会锁定。界面只在收到主进程确认后显示保存成功。

## 配置主、副 U 盘

入口位于左侧「加密导入导出」。应用外观、锁屏时间和继承说明位于左侧「系统设置与安全」。

1. 插入两块不同的物理 USB 存储设备，点击扫描。
2. 选择主盘 A、副盘 B。同一物理盘的两个分区不能作为两盘。
3. 点击“配置主、副 U 盘”，重新输入所有者密码和密钥。
4. 应用向独立目录写入两份签名恢复秘密和加密密库，读取验证后才确认配置成功。不会格式化设备。

目录结构：`LegacyLock/<密库 ID>/<代次>/`。主盘有 `primary.llkey`，副盘有 `secondary.llkey`，两盘均有 `vault.llvault`。请保留完整目录，并分开保管两盘。真实设备身份检查用于避免误选同一盘，不能阻止复制密钥文件。

重新配置或更换两盘会生成新的恢复秘密和新的数据密钥；失败时保留上一代本机配置。旧备份仍可能被旧盘打开，不能远程撤销已经交付的历史明文。更换成功后不要混用不同代次密钥。

## 备份和导出

**每次编辑只保存本机。离线 U 盘不会自动更新。** 编辑或设置完成后，插入两盘，点击“同步当前密库到两盘”。界面会显示本机版本，以及本次会话是否确认两盘同步该版本。没有确认就应视为备份状态未知。

“验证凭据并导出加密备份”使用本机当前密码和安全密钥。若要改变导出凭据，先更换密码和安全密钥，再重新导出和同步两盘。旧导出包继续使用旧凭据，修改本机设置不会追溯更改旧文件。取消保存对话框不算成功。

更换凭据不撤销双盘读取权限；若恢复设备遗失，应重新配置两盘。两盘恢复权与所有者管理权是不同能力。

## 迁移和异常

见[开发与恢复文档](DEVELOPMENT.md)。新密库文件损坏或读取失败时，应用不会创建空数据覆盖它。请保全原文件和 `.previous`，再进行恢复。不要反复初始化或删除用户数据目录。

没有“安全销毁全部副本”按钮。磁盘、USB、系统快照、SSD 和备份中的历史副本无法由应用保证物理擦除。查看或下载附件会产生可独立保存的明文副本，请自行妥善管理。

## 背景与订阅测试（2026-09-21）

顶部「主题配色」和设置页可选择原有五套渐变背景。设置页支持 85%、100%、115%、125% 内容缩放。设置成功后保存在本机，重启沿用；继承只读会话不能修改外观、订阅或应用设置。资产列表恢复分类侧栏、搜索、卡片、密码显示与复制、附件下载，添加资产先选择原有分类模板。

顶部「订阅测试」或设置页「付费订阅 · 测试体验」提供月度 ¥28、季度 ¥68、年度 ¥168 示例方案。没有真实支付、扣费、自动续订或正式授权验证。点击「模拟试用到期」后，本机主进程暂停资产新增、修改和删除，但所有者仍可导出、查看和管理设置。点击「恢复试用体验」或「模拟开通所选套餐」恢复所有者编辑。演示状态不会提升继承人的权限，也不是防篡改商业授权。

主题、缩放和订阅演示状态保存在本机 `appearance-v1.json`，不写入密库格式、不随导出包迁移。该配置损坏时使用临时默认外观并提示，不阻止解锁密库、不自动覆盖原配置。卡片复制成功后由主进程在 30 秒或锁定时清除本次复制内容；如果剪贴板已被其他内容替换则不清除。系统剪贴板历史或其他程序保留的副本不在此范围。
