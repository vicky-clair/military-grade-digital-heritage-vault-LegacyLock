# 🛡️ LegacyLock 军规遗产密钥库 — 所有者使用手册 (User Manual)

> **文档定位**：密库创建者、资产所有者日常管理与安全配置完整实操指南  
> **适用版本**：LegacyLock v2.5.0 LTS (Military Build)  
> **安全标准**：LLCS-1 (AES-256-GCM + PBKDF2-100k + X25519 双向协商)  
> **保密级别**：100% 纯离线冷存储 · 零云端依赖 · 绝无网络外联 · 三语原生适配

---

## 目录
1. [系统简介与设计哲学](#一-系统简介与设计哲学)
2. [环境准备与一键启动](#二-环境准备与一键启动)
3. [多语言切换与高分屏显示自适应](#三-多语言切换与高分屏显示自适应)
4. [所有者日常资产录入与管理](#四-所有者日常资产录入与管理)
5. [双密码体系与安全配置](#五-双密码体系与安全配置)
6. [锁屏防护与剪贴板自毁](#六-锁屏防护与剪贴板自毁)
7. [常规加密备份与导入恢复](#七-常规加密备份与导入恢复)
8. [制作双 U 盘（主副介质）与异地封存](#八-制作双-u-盘主副介质与异地封存)
9. [高安全专属：纯冷存物理模式 (Zero Footprint)](#九-高安全专属纯冷存物理模式-zero-footprint)
10. [介质健康自检与无损平滑迁移](#十-介质健康自检与无损平滑迁移)
11. [生前交付规划与纸质密封单](#十一-生前交付规划与纸质密封单)
12. [3 个月免费试用与柔性服务权益说明](#十二-3-个月免费试用与柔性服务权益说明)

---

## 一、 系统简介与设计哲学

在数字化时代，助记词、银行凭证、服务器私钥与核心账号分散在各个角落。传统的云端密码管理器存在服务商跑路或被攻破的单点风险；而纯本地明文保存又无法在身后安全交付给法定继承人。

**LegacyLock** 专为解决这一终极命题而设计：
- **生前日常使用**：数据在本地电脑加密保存，所有者凭借一个 **Master PIN** 即可快速解锁与管理，无需每天翻找 U 盘；
- **身后传承接管**：通过 **主副双 U 盘物理隔离（2-of-2 Threshold）**，单凭任何一个 U 盘在数学上绝对无法破解，必须两块介质在身后同时插入电脑，配合 **Heir PIN** 方可联合解密，有效防范提前偷看与失窃风险；
- **绝对纯离线（Air-Gapped）**：全系统无中心化服务器，绝不发起任何网络请求，内部数据出不去，外部网络进不来。

---

## 二、 环境准备与一键启动

### 1. 系统要求
- **操作系统**：Windows 10/11、macOS 12+、Ubuntu 20.04+（三大平台原生适配）；
- **硬件要求**：最低 512MB 可用内存，无网络连接需求（纯离线）；
- **运行环境**：最终交付可直接使用单文件免安装便携版（Zero Node.js Prerequisite）。

### 2. 桌面客户端启动方式
- **方式一：单文件绿色便携版 (推荐，完全零环境依赖，约 63MB)**：
  - 直接双击 `release/LegacyLock-v1.0.0-Windows-Portable.exe`，无需安装，即点即开；
- **方式二：解压免安装版**：
  - 直接运行 `release/win-unpacked/LegacyLock.exe`；
- **方式三：安装向导版 (约 63MB)**：
  - 双击 `release/LegacyLock Setup 1.0.0.exe`，自动创建桌面与开始菜单快捷方式；
- **方式四：源码与脚本启动**：
  - Windows 用户双击 `启动LegacyLock.bat`；
  - 开发者在终端执行 `npm run electron`。

---

## 三、 多语言切换与高分屏显示自适应

### 1. 三语国际化界面切换 (English / 简体中文 / 日本語)
- **默认语言**：遵循国际规范，系统首次启动严格**默认为英文界面 (English)**；
- **切换语言**：
  1. 点击左侧导航底部常驻的 **「Settings (系统设置)」**；
  2. 找到 **「Language & Regional (语言偏好)」** 下拉选择菜单；
  3. 支持在 **English**、**简体中文** 与 **日本語** 之间毫秒级无缝切换；
  4. 切换后即刻全量生效，并在跨会话与重启后永久持久化保留。

### 2. 显示自适应与全局缩放 (Display Scaling & Zoom)
为了在超小型便携笔记本、高分屏（4K/2K）以及大屏台式机上均获得舒适阅读体验，系统内置完整的显示自适应控制：
- **小屏智能最大化**：当检测到屏幕垂直分辨率较低（< 768px）或笔记本紧凑屏幕时，应用在启动时自动最大化展开，确保所有内容完整展示；
- **顶栏双击最大化/还原**：双击软件顶部任意空白区域，可随心切换窗口全屏最大化与居中还原；
- **全局缩放快捷键**：
  - `Ctrl` + `+` 或 `Ctrl` + `=`：**放大界面** (每次递增 5%，最高可至 200%)；
  - `Ctrl` + `-`：**缩小界面** (每次递减 5%，最低可至 60%)；
  - `Ctrl` + `0`：**快速还原为 100% 默认标准缩放**；
  - `Ctrl` + **鼠标滚轮**：平滑随动缩放。
- **设置面板滑块控制**：在「系统设置」->「显示与界面缩放」中，通过滑块与预设按钮（80% / 90% / 100% / 110% / 125%）一键微调。

---

## 四、 所有者日常资产录入与管理

系统支持 12 种核心资产类型的结构化管理，每种类型均具备专属字段模型：

```text
[登录凭据]     [安全备注]     [身份凭证]     [银行金融]
[独立密码]     [加密文档]     [SSH私钥]     [API凭据]
[加密钱包]     [主机服务]     [路由网关]     [会员资产]
```

### 1. 录入新资产
1. 点击主界面左上角高亮的 **`[+ 添加新资产 / 密钥]`** 按钮；
2. 弹出「你想要添加什么？」资产分类选择器，点击对应卡片；
3. 在表单中输入资产标题、账号、密码、密钥、URL 网址或私密备注：
   - **军规级真随机密码发生器**：点击密码栏上方的「生成 20 位军规密码」，系统通过底层操作系统真随机熵池（CSPRNG）配合无偏拒绝采样算法生成高强度密码；
   - **自定义键值对**：支持按需添加专属自定义属性（如“PIN 码”、“二次验证恢复码”、“助记词路径”等）；
4. 点击 **`[保存资产项]`**，系统即刻在本地使用派生密钥经 `AES-256-GCM` 认证加密保存。

### 2. 检索与日常查阅
- **分类筛选**：点击左侧导航栏分类标签（数字徽标实时统计当前分类资产总数）；
- **实时搜索**：顶部搜索框输入关键词，支持按标题、账号或备注毫秒级模糊匹配；
- **密码防窥视保护**：资产列表默认以 `••••••••` 掩码显示密码，点击眼睛图标即可明文查看；切换分类或离席锁屏时会自动闭合所有展开的密码。

---

## 五、 双密码体系与安全配置

LegacyLock 的安全性基石在于**双密码隔离**，切勿将两个密码混淆：

| 密码名称 | 作用与适用场景 | 知晓范围 | 存放与保管建议 |
| :--- | :--- | :--- | :--- |
| **所有者主密码 (Master PIN)** | 用于所有者日常电脑锁屏唤醒、本地密库查阅与日常修改。 | **仅所有者本人知晓**，绝不透露给任何人（包括继承人）。 | 印在大脑记忆中，严禁写在便签或保存在手机相册。 |
| **继承人接管口令 (Heir PIN)** | 留给法定继承人身后配合双 U 盘进行联合激活解密的口令。 | **仅供继承人身后使用**。 | 密封在信封中放入律所遗嘱卷宗或公证书中。 |

### 配置方法：
1. 点击顶部工具栏的 **`[🔑 U盘密码]`** 按钮；
2. 左右双卡并列展示：
   - 左卡输入设置 **所有者主 U 盘密码 (Master PIN)**；
   - 右卡输入设置 **法定继承人接管口令 (Heir PIN)**；
3. 点击 **`[保存双钥匙密码配置]`**，系统自动生成 16 字节真随机 Salt，并经过 PBKDF2 100,000 轮单向哈希固化。

---

## 六、 锁屏防护与剪贴板自毁

### 1. 随手立即锁屏
- 离席去洗手间、开会或离开电脑前，随手点击顶部快捷工具栏的 **`[🔒 立即锁屏]`** 按钮；
- 界面立即弹出深色高斯模糊全屏遮罩，彻底遮盖所有资产详情。

### 2. 无操作自动锁屏
- 点击左侧底部的 **「系统设置」** 入口；
- 在「高级实用安全偏好」中配置“无操作自动锁屏”时间（推荐设置为 15 分钟）；
- 当键盘与鼠标静止超过设定时间，全屏锁屏自动激活；
- **唤醒验证**：锁屏界面**强制验证 Master PIN**，输错直接阻断，输入 Heir PIN 也无法唤醒。

### 3. 30 秒敏感剪贴板自动销毁
- 在复制私钥、助记词或密码后，系统后台自动挂载 30 秒倒计时；
- 30 秒后自动静默擦除系统剪贴板，彻底杜绝本地后台木马的剪贴板嗅探。

---

## 七、 常规加密备份与导入恢复

当您需要更换新电脑、重装系统或异地备份时，使用导入导出功能：

### 1. 军规加密导出 (融合军规级 Secret Key 双重加固)
1. 点击左侧导航栏的 **「导入导出」** -> 选中 **「军规加密导出」**；
2. 设置导出加密口令（或点击一键生成 20 位军规口令）；
3. 系统自动融合您在首次使用时生成的 **128 位物理安全密钥 (Secret Key)** 进行双因子混合加固；
4. 点击 **`[立即执行军规加密导出]`**；
5. 系统生成包含认证标签的 `.legacylock` 密包文件并下载至本地；
6. 该密包受 AES-256-GCM + PBKDF2 双因子强加密保护，即使他人偶然窥视到了您的导出密码，若没有物理救援卡上的 Secret Key，在数学上也绝对无法破解！

### 2. 解密验证导入 (需密码 + 紧急安全密钥)
1. 在新电脑上重装应用后，打开 LegacyLock，点击 **「导入导出」** -> 切换至 **「解密验证导入」**；
2. 将 `.legacylock` 密包文件拖入上传区域；
3. 输入导出时设置的加密口令；
4. 输入您的 **紧急安全密钥 (Secret Key)**（格式如 `LL-XXXX-XXXX-XXXX-XXXX-XXXX`）；
5. 选择合并策略：
   - **安全增量合并（推荐）**：保留当前密库已有项，去重追加新内容；
   - **完全覆盖密库**：清空当前库，以导入文件为准；
6. 点击 **`[验证口令并解密导入]`**，全量资产瞬间安全恢复。
> 💡 **防盗特别保障**：继承者或黑客即便窃取了您的主密码，在重装导入时没有这张实体纸质卡片上的 Secret Key，系统强制拒绝解密，彻底杜绝数据被盗恢复！

---

## 八、 制作双 U 盘（主副介质）与异地封存

为了建立牢不可破的数字遗产物理冷存储，所有者应制作双钥匙介质：

### 1. 硬件选型建议
- 准备 **两块高品质外接存储设备**（普通 USB 3.0 闪存盘、移动固态硬盘 SSD 或移动机械硬盘 HDD 均可）；
- 将第一块盘命名/贴标为：**`[所有者主盘 A]`**；
- 将第二块盘命名/贴标为：**`[继承人副盘 B]`**。

### 2. 生成与写入钥匙
1. 将两块 U 盘同时插入电脑；
2. 打开系统设置中的「外部存储硬件与介质识别中心」，确认检测到两个外部盘符（如 `D:\` 与 `E:\`）；
3. 点击主界面的 **`[存储至移动介质]`**：
   - 系统将所有者钥匙 `user-key.bin` 写入主盘 A；
   - 系统将继承人钥匙 `heir-key.bin` 与加密密包 `vault.locked` 写入副盘 B；
4. 将软件的免安装便携版复制一份至副盘 B 根目录。

### 3. 异地物理隔离封存（黄金法则）
- **主盘 A（所有者盘）**：放入书房机械密码保险箱，或随身携带；
- **副盘 B（继承人盘）**：交付给法定继承人随身保管，或存入银行保管箱；
- **关键保障**：**两盘异地分离**，继承人手里只有副盘 B，在数学上绝无解密可能，彻底杜绝生前提前偷看。

---

## 九、 高安全专属：纯冷存物理模式 (Zero Footprint)

> **适用人群**：与子女/他人共用电脑、极度警惕身边人翻看电脑的高安全需求所有者。

如果您希望日常电脑上“**绝对不留任何密库痕迹，拔盘即走**”：
1. 在主电脑上录入并同步完数据至主盘 A 后；
2. 进入系统设置，滑动到底部「危险区域」；
3. 输入确认指令 `ERASE-ALL`，点击 **`[一键安全销毁]`**；
4. 拔掉主 U 盘 A 并锁入保险箱。

**防御效果**：
- 此时您电脑的磁盘中**没有留存任何加密资产数据**；
- 继承人哪怕偷偷溜到您电脑前，打开软件也是一张白纸；
- 只有当您需要使用时，临时插入主 U 盘，软件才从外接介质加载，拔出即走，实现真正的“寸草不留”。

---

## 十、 介质健康自检与无损平滑迁移

### 1. 运行 6 项密码学完整性自检
- 点击顶部快捷工具栏的 **`[🛡️ 密库自检]`** 按钮；
- 系统自动对密库进行全链路自检：容器版本规范、AEAD 认证标签哈希校验、X25519 密钥对配对检测、失效时间戳与世代防回滚状态；
- 自检全绿（`HEALTHY`）代表密库处于绝对健康状态。

### 2. 闪存防老化与无损平滑迁移
- **闪存电荷流失预警**：普通闪存 U 盘断电存放 5~10 年可能会出现微弱电荷泄漏；
- **建议维护周期**：每 12~18 个月将主副 U 盘插入电脑，在设置中运行自检，并点击 **「介质无损迁移与克隆升级向导」**，将数据重新覆写刷新，或无损迁移至新的高速固态硬盘。

---

## 十一、 生前交付规划与纸质密封单

1. 进入系统设置，点击 **`[📄 打印纸质应急留档单 (Paper Key Sheet)]`**；
2. 系统生成包含密库唯一标识码（Vault ID）、继承人姓名、使用指南的标准 A4 卡片；
3. 手写补充必要的保管线索（例如：“副盘放在银行 082 号保管箱，主盘在书房暗格”）；
4. 装入防静电铝箔袋中，采用防水防火密封袋二次封装，随公证遗嘱或家族信托公约永久归档。

---

## 十二、 3 个月免费试用与柔性服务权益说明

LegacyLock 采用尊重用户隐私与数字遗产主权的商业模式：
- **开箱享 3 个月全功能免费试用**：首次使用即可享受全部军规加密、资产录入与双 U 盘冷存制作功能；
- **柔性到期保障（永久不扣押用户数据）**：
  - 试用到期后，系统仅对“新增资产”与“修改现有资产”施加柔性提示；
  - **既有数据永久属于用户本人**：即使未续订，用户随时可以使用 Master PIN 或双 U 盘解密打开密库，**100% 完整查阅所有密码明文、复制凭据、以及完整执行军规加密导出**；
  - 绝不存在“不付费就删除数据或锁定只读无法提取”的恶意绑架行为。
