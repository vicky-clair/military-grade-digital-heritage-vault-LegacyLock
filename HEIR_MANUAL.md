# 继承人使用手册 · LVCF 3

## 在原电脑读取

1. 安装或启动本版本应用，插入配置成功的主、副两盘。
2. 锁定页面点击扫描，选择两个不同的 USB 设备。
3. 点击“用双盘打开本机密库”。应用读取两份恢复秘密，验证所有者签名，再解密数据。

成功后显示“继承人 · 只读”。可以搜索、查看全部字段、阅读继承说明、下载单个附件。不能修改或删除资产、修改应用设置、更换凭据、重新配置两盘或导出所有者备份。后端也会拒绝这些操作，不依赖按钮是否可见。

## 在其他电脑导入

1. 在新电脑安装应用，插入两盘并扫描选择。
2. 点击“选择备份导入并只读访问”。
3. 在任一盘 `LegacyLock/<密库 ID>/<代次>/` 中选择 `vault.llvault`。

导入的是已签名加密快照。重启应用仍为锁定状态，不会自动变成所有者。再次访问仍需两盘，或同时提供所有者密码和安全密钥。

如果本机已存在另一个密库，导入会被拒绝以防误覆盖。请使用独立系统账户恢复。比本机版本更旧的备份也不能直接覆盖本机密库。

拔出任一盘会在后台设备检查完成后锁定；设备扫描有延迟，并非瞬时硬件断电隔离。离开电脑时应主动点击“立即锁定”。

## 获取管理权限

点击“输入密码和密钥，接管管理权限”，必须同时提供正确的所有者密码和完整安全密钥。双盘恢复材料不包含所有者签名私钥，因此没有两项凭据时不能签署原密库的新版本。

只读保护针对本应用与原密库签名。获得明文的人可以另存或编辑自己的副本；软件不能限制已经被读取的信息，也不自动判断死亡、法律继承资格或遗嘱效力。

## 独立离线阅读器

当桌面应用无法运行时，可保留 Node.js 24 与以下源文件：`scripts/recovery-reader.cjs`、`electron/vault-core.cjs`、`electron/vault-store.cjs`。它们没有第三方 npm 运行时依赖。

打包安装目录的 `resources/reader/` 也包含这些文件与协议文档；macOS 在应用包 `Contents/Resources/reader/`。建议随备份额外保管完整 reader 目录及可用的 Node.js 运行时。

```sh
node scripts/recovery-reader.cjs <vault.llvault> <primary.llkey> <secondary.llkey> <新的恢复输出.json>
```

阅读器验证签名并恢复全部数据，包括附件的 base64 内容。输出是**明文**，输出路径必须不存在，不会覆盖原密库。阅读器按两份秘密工作，不要求操作系统将文件识别为两块 USB；这用于跨系统抢救，不能被解释为不可复制的硬件保护。格式没有到期日和在线激活要求。

旧版 `user-key.bin` / `heir-key.bin` 与新格式不兼容。旧备份缺陷不能通过更新软件消除，见[迁移说明](DEVELOPMENT.md)。
